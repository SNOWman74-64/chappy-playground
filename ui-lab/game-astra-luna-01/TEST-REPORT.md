# Test report

## 条件

試行B astra-luna-01。ユーザーの追加指示により、Astraがブラウザ接続・全検証を担当し、Lunaは実装・修正に専念する。実装担当自身による検証という当初条件から変更されている。元の接続失敗はlogs/run-before-role-change.jsonに保持。

## 環境

Astraのブラウザでenvironment.htmlの画像、入力、Tab、Enter、確認表示、スクリーンショット取得を確認。固定入力チェックPASS。Lunaのmodel/highはセッション記録で確認、Fastは起動要求として記録。実配信tierは取得不能。

## 途中レビュー

1回目実施済み。トップとc01詳細を1440×1000、390×844で確認。logs/review1.mdと対応するscreenshotsを参照。レビュー前のstage1-before-review.zipを保存。Lunaによるブラウザ自己検証は実施していない。

## 完成時受入れ

未検証。第二段階の実装後にAstraが記録する。設計・実装の自己申告のみでPASSにしない。

## 初回完成版の受入れ結果（修正前）

| ID | Astraの結果 | 証拠・範囲 |
| --- | --- | --- |
| ENV | 条件変更のうえPASS | Astra接続、Luna接続は失敗のまま |
| V01 | FAIL | 図鑑390pxでscrollWidth807px。他の対象5画面ははみ出しなし |
| V02 | 採点済 | review2.md、PC4/4/4/4、mobile4/4/3/3 |
| D01 | PASS | 火+支援c05の1件、検索0件、reset12件 |
| D02 | PASS（実施範囲） | 評価順c01..c12、名前順表示、c01数値と戻る/進む |
| S01 | PASS | 結晶26件と該当URL、存在しない0件 |
| A01 | PASS | g01保存・解除・再保存・再読込・保存一覧。目次は別の不具合として記録 |
| T01 | PASS | 4枠、満員無効、重複を作れないUI、1枠削除後カエデ追加 |
| T02 | PASS（実施範囲） | 新タブ4枠復元、不正/重複ID説明。復元後編集は修正対象 |
| B01 | PASS | 質問t01,t03、返信数順2,2,1,1,0,0 |
| B02 | PASS（実施範囲） | 空白/81文字タイトル拒否、入力確認修正投稿保持。全境界値の網羅は未実施 |
| B03 | FAIL | 返信編集保持。削除確定直後は1件のまま、再読込後0件 |
| B04 | PASS | 指定HTML文字列を表示しimg要素0、alertなし |
| B05 | PASS | 理由選択後デモ完了、元投稿維持。外部送信なしはコード確認を併用 |
| P01 | PARTIAL | 取消維持・確定初期化・6seed復元。別キー保持の実ブラウザ注入は未検証 |
| P02 | PARTIAL / FAIL | VMで構文破損/拒否は合格、ネスト破損は例外。実ブラウザ障害注入未検証 |
| K01 | PARTIAL | mobileメニューEscape/遷移閉鎖、接続ページTab/Enter確認。最終画面の重点キーボード検査は修正後 |
| R01 | FAIL | 目次クリックで見つからない画面。主要詳細は表示、残り直接URLは修正後に検査 |

操作記録はlogs/acceptance-browser.json、画面はlogs/screenshots/initial-*。VM試験はlogs/storage-initial.json。初回の結果は修正後で上書きしない。

## 修正後の確認結果

レビューは予定どおり2回。2回目後の修正再検証で820pxヘッダーの重なりと390px編成名の省略を検出し、CSS修正を1往復追加した。これは新しい設計提案ではなく受入れ不具合の修正として記録。全試行履歴はlogs/に保持。

- V01：6画面×1440/390/820の18画面を保存。全サイズで本文の横はみ出し・画像欠落を検出せず。図鑑390pxはscrollWidth375（スクロールバー込みビューポート390）へ修正。820pxヘッダーの操作重なり、390px編成名の省略は追加修正後の画像で解消を確認。
- D01/S01/A01/B02/B04：図鑑1/0/12、検索26、記事保存解除再保存再読込、投稿確認から戻る→投稿→再読込、HTML文字列とimg要素0を再検証。
- B03：返信削除確定直後に0件へ更新。自分の親投稿の編集・再読込・削除取消・削除確定も確認し、掲示板が6件へ戻った。
- T02：共有URL復元後にアカリを外して3枠へ変更可能。復元が操作のたびに再実行される問題を解消。
- P01：取消と確定を再検証。保存一覧・デモ投稿を初期状態へ復元。別キーを仕込んだ実ブラウザでの保持検証は未実施。
- P02：Node VM補助試験でJSON構文破損、書込拒否、userThreadsのネスト破損の3ケースが合格。これは実ブラウザ障害試験の代替合格ではない。実ブラウザでの保存拒否・破損の再現は未検証。
- K01：最終実装のスキップリンクをEnterで実行しmain-contentにフォーカス、検索欄からTab→Enterで検索26件へ遷移。モバイル/タブレットメニューのEscapeと遷移後閉鎖を確認。すべての操作をキーボードのみで網羅した試験、スクリーンリーダー試験は未実施。
- R01：c01/c12、g01/g08、イベント全3件、t01/t06の直接URLが正しい画面。不明IDは各種別で見つからない案内。目次は#guide/g01?section=1へ遷移して記事を維持。全IDの網羅検査は未実施。
- リソース19件はHTTP200。ブラウザconsole warn/errorは採取時0件。全ネットワーク要求を網羅するHARは未取得。
- node --check合格。共通入力チェック合格。共通静的チェックはギャラリー未登録のみFAIL。実験READMEに従いカタログは編集していない。

### 最終の判定

通常操作と検出不具合の修正は完了。P01/P02/K01/R01は上記の未検証範囲を残すため、受入れ基準の完全合格とは断定しない。コピー成功と選択可能URLは確認したが、コピー拒否時の選択復元はコード確認のみで実ブラウザ再現未検証。Fastは要求設定であり実配信tierは不明。

### 保存物

- logs/snapshots/stage1-before-review.zip：第一段階レビュー前
- logs/snapshots/initial-complete-before-review.zip：初回完成・独立完成レビュー前
- logs/snapshots/final-after-corrections.zip：修正・再検証後
- logs/screenshots/：段階別・サイズ別の画像
- logs/acceptance-browser.json：操作とDOM記録
- logs/metrics.json：親子別・合計の時間とトークン、集計境界
- run.json：試行状態・条件変更・検証範囲

実費は請求情報未取得のためnull。commit・push・公開は実施していない。他試行の実装・評価は参照していない。
