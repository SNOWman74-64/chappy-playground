# Review 2 — 初回完成版

Astraが全検証を担当。初回完成提出はlogs/stage2-final.txt。レビュー前にinitial-complete-before-review.zipを読み取り専用保存。screenshots/initial-*、acceptance-browser.jsonに実ブラウザ証拠。

## 視覚評価

| 観点 | PC1440 | モバイル390 | 根拠 |
| --- | --- | --- | --- |
| 見出し・説明・操作 | 4 | 4 | ヘッダー修正でPCが1行、主要入口が明確 |
| 情報階層 | 4 | 4 | モバイルキャラ詳細がコンパクトになり操作が第一画面に入る |
| 画像の役割 | 4 | 3 | 文字との分離は良い。モバイルイベント画像のトリミングが強い |
| 文字組み | 4 | 3 | 記事の各行は改善。図鑑ツールバーだけ横はみ出しが発生 |

## 修正必須

1. V01：#characters 390×844でdocument scrollWidth807px、表示幅390px。絞込みツールバーがはみ出し、フォーム高も約100px。グリッド列とfirst-child spanをモバイルで明示リセット、入力高44px程度、min-width0。図鑑以外の5対象画面は横はみ出しなし。
2. B03：t01へ返信→編集→削除取消→削除確定で、直後は返信本文・1件が残る。再読込で0件になる。bindModalActionsの削除後は同じhashでも必ず再描画する。
3. R01/A01：g01目次「進め方」をクリックすると#guide-g01-section-1へ移り見つからない画面。記事ルートを維持してスクロールするか、sectionをクエリで表現して直接復元する。
4. P02（補助VM試験）：JSON構文破損と書込拒否は一時モードへ移行するが、validStoreに正しい外形かつuserThreads:[null]を渡すとallThreadsが例外。配列要素やeditsを含むネストデータを検証・安全化し、破損なら復旧案内を出す。
5. P02（コード確認）：編集・削除・編成・resetはsaveStore/removeItem失敗でも成功のようなメッセージ。永続保存失敗と一時状態変更を区別し、成功と誤認させない。ブックマーク拒否時も一時利用できる設計にする。
6. コピー失敗時input.select後のrenderで選択DOMを捨てている。再描画後に新しいURL入力をfocus/selectする。
7. 共有URLの復元/addパラメータは初回のみ消費し、編成削除や追加操作ごとに復元・再追加しないようにする。

## 確認済み

検索26/0、図鑑火+支援c05/0/12、評価順、詳細一致とback/forward、g01保存解除再保存再読込、4枠追加/満員/1枠削除後追加、共有新タブ復元/不正URL説明、質問2件/返信数順、空白/81文字拒否/確認戻り値保持/投稿/再読込、HTML文字列表示、返信追加編集永続化、通報で元投稿維持、初期化取消/確定で6seedへ、モバイルメニューEscape/遷移閉鎖。

## 検証限界

実ブラウザによる保存拒否/破損の強制注入は未検証。VM試験とコード確認を分離。コピー拒否の実ブラウザ再現も未検証。初期化が他キーに触れないことはコードで対象キーを確認したが、他キーを仕込む実ブラウザ試験は未検証。完成レビューはこの1回で終了。以後は指摘修正の再検証と残り受入れ項目の検査として記録する。
