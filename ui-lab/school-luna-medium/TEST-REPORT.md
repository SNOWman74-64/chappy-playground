# Test Report

## Parent browser verification after repairs

2026-09-07、親担当が実ブラウザで検証。1440px/390pxの主要15ルートで横あふれなし。トップの実画像読込みと両幅のスクリーンショットを確認。問い合わせの必須/メール形式、入力値を含む確認、値を保持して戻る、デモ完了を確認。予約も確認/修正/完了と、別日程への切替（10月10日→11月7日）を確認した。文化部4件、入試ニュース2件への絞込み、部活動の詳細直接表示、記事詳細遷移、検索3件/0件、FAQのEnter開閉、モバイルメニューの開閉/Escape/遷移後閉鎖を確認。トップ再読込でpageerrorなし。ブラウザ生結果は../school-comparison/logs/school-luna-medium-browser.json、画面は同logsのdesktop/mobile.png。

共通静的チェックは比較登録後に全10study PASS。初期のカタログ未登録エラーは統合で解消。モバイル検索ボタンの文字は縦に折り返すため、視覚上の改善余地として残る。記事本文の重複など編集品質はこの操作チェックの合格範囲外。

## Scope

静的ファイルの実装確認と、コード上の主要導線・インタラクション確認。

## Executed

- `scripts/check-ui-study.ps1` は、この study ディレクトリに存在しないため未実行。
- ブラウザQA（1440px/390px、横あふれ、画像、実行時エラー、キーボード、フォーム実操作）は親担当が実施するため未検証。
- トップ、学校紹介、学び、学校生活、部活動、入試・説明会、お知らせ、アクセス・FAQ、検索、問い合わせ、資料案内、フッター案内のルートを用意した。
- ニュース8件、部活動8件、施設6項目、説明会3回、FAQ6問をコードで確認した。
- 親QA指摘1: 問い合わせは `contactState` に全入力値を保持し、種別・氏名・メール・内容を確認画面に表示。編集で入力画面へ戻り、値と同意状態を復元し、デモ完了画面へ進む実装に修正。
- 親QA指摘2: 説明会予約は `reserveState` に参加回・氏名・メール・人数・同意状態を保持。確認画面にメールを含む全項目を表示し、修正後も値を復元する実装に修正。
- 親QA指摘3〜5: 8部活に詳細/一覧戻り導線、8記事に日付・分類・タイトル・本文詳細、在校生/卒業生の実用案内、リンク付きサイトマップ、入試・フッターから到達できる印刷用学校案内を追加。
- 親QA指摘6〜7: `esc()` でユーザー入力をHTML属性・本文へ埋め込む箇所をエスケープ。検索対象をニュース・部活・施設を含むページ本文へ拡張し、記事/部活詳細へリンク。壊れたURIは空検索へフォールバックし、`#main` はルート扱いせずスキップ先として処理。ルート遷移ごとにフォーカスと先頭スクロールを設定。
- `node --check script.js` は修正後も成功。
- 追加修正: `#/reserve/{id}` の明示イベント遷移では常に対応する日程へ更新し、IDなしの `#/reserve` 編集遷移では現在の日程と入力値を保持するように修正。

## Known limitations

ブラウザでの視覚・操作証拠は未取得。今回の修正後も、フォーム確認/編集/完了、予約値保持、検索の特殊文字、壊れたハッシュ、モバイル表示は親担当のブラウザQAで確認する。

## Evidence status

親担当が実行するブラウザQA待ち。
